================================================================================
 BYOND DM LANGUAGE TOOLING - ROADMAP
 Live doc. The SHORT version: status, what is next, and where the detail is.
 Full detail in PLAN.md. Integration detail in INTEGRATION.txt.
 Last updated: 2026-08-11
================================================================================

 This file went to 1,010 lines by restating PLAN.md in different words, and the
 two then disagreed with each other twice. It is a status board now. Anything
 stated once in PLAN.md is POINTED AT from here, not repeated - see WHERE THE
 DETAIL LIVES at the bottom.


WHERE WE ARE
--------------------------------------------------------------------------------
  M0-M10 COMPLETE. M9 past its target. M11 started, and holding zero invented.
  M8 was passed over at the M8/M9 fork and landed 2026-08-08.

    dm_core.dll     2.29 MB, ABI 0.28, 39 exports, cdecl on every platform
    six RIDs        win-x64 win-x86 linux-x64 linux-arm64 osx-x64 osx-arm64
    tests           1,345 green (1,276 core, 44 native, 25 lsp)
    BYOND 516.1686  the goldens are STAMPED to it and CI runs against it.
                    516.1686 is the newest build (2026-08-05); the suite is
                    green on it end to end, tripwire included
    abi-smoke       258 checks from C++, reporting 0.28 on win-x64 AND win-x86
                    - both re-published and re-run locally 2026-08-13. FIVE of
                    six RIDs were green in CI at 0.21
                    - win-x64, win-x86, linux-x64, linux-arm64, osx-arm64.
                    osx-x64 was UNVERIFIED until 2026-08-07: it asked for the
                    retired macos-13 image, and a job whose label no longer
                    exists queues forever rather than failing - it sat unstarted
                    for an hour and read as slow. Now on macos-15-intel. The
                    Unix RIDs are CI-only either way, since NativeAOT refuses
                    cross-OS native compilation
    mlaas           EXACT against dm.exe -o: 1493/1493 vars, 1153/1153 procs
    madridspy       exact both ways
    tgstation       99.99% vars, 99.92% procs
    include graph   102/102 against dm.exe -l, order identical after M9
    one keystroke   10 ms on mlaas (target was 30), 909 ms on tgstation
    diagnostics     ZERO INVENTED EVERYWHERE - mlaas 0, madridspy 0, and
                    tgstation 0 from 1,392: dm.exe says nothing on 1.5M
                    lines and now so do we
    fixture suite   dotnet test + pwsh tests/fixtures/run.ps1, 255-probe ratchet
                    at 74/255 agreed
                    - and it now runs IN CI against the real compiler, which it
                    never did before: the byond-fixtures job fetches the
                    standalone zip, so a BYOND release that changes what DM
                    means fails a build instead of waiting to be noticed

  Shipping through the ABI today: syntax highlighting (lexical AND semantic
  colours 12-15), document outline with syntax diagnostics, completion,
  go-to-definition, hover, signature help, workspace symbol search, and bulk
  object-tree queries. Buffers, injected defines and both position encodings
  throughout.

  No commit count here on purpose - it is wrong again the moment anything is
  committed, which is how the last one got stale.


WHAT WE ARE BUILDING
--------------------------------------------------------------------------------
  Dm.Core     C# library that parses and understands DM. ~85% of the work,
              shared by every shell.
  Dm.Native   NativeAOT wrapper -> dm_core.dll, the C ABI for the Qt IDE and
              anything else with a C FFI.
  Dm.Cli      dmc, the dev driver. Fastest debug loop and the arbiter when an
              IDE reports a bug: if the CLI reproduces it, it is ours.
  Dm.Lsp      LSP server for VS Code / Neovim / everyone else.        [M10]
  Dm.Assets   .dmi icon state reader. DONE at M8.


WHAT'S NEXT
--------------------------------------------------------------------------------
  THE QUEUE, as of 2026-08-08. Detail for each is in PLAN's changelog and in
  the section it belongs to; this is the order.

    a. DONE 2026-08-08: the COLOUR PROVIDER, at ABI 0.23. rgb() calls and
       hex literals, on all three surfaces plus `dmc colors`. Named colours
       and the non-RGB colour spaces are deliberately out - see PLAN 8.
    b. DONE 2026-08-08: M8, the .dmi icon states, at ABI 0.24.
    c. dm.exe's DEFAULT-ON WARNINGS - the missed column, below. The grouping
       is DONE and THREE OF THE FOUR SHIPPED. The prior was wrong:
       unused_var is 2 of 16, not the bulk.
         DONE  new_name (4005)  lentext deprecation, 9 lines
         DONE  no_parent (3013) ..() with nothing above it, 4 lines
         DONE  unused_var       ATTEMPT FOUR, 2026-08-12, ZERO INVENTED on all
                                four projects. The three previous attempts
                                blamed a missed STATEMENT shape and that was
                                never it - BindStatement handles all sixteen
                                kinds. The walk had NO NOTION OF A LOCAL BEING
                                READ: BindIdentifier returns early when the
                                sink is null (every diagnostics run) and again
                                exactly when the name IS a local, since the
                                reference index excludes them.
                                Six EXPRESSION holes then fell out of tgstation
                                one site at a time, and FIVE were hiding uses
                                from the reference index too: a for header's
                                RangeEnd/Step, a label's BODY, an argument's
                                assoc key and weight, modified-type entries,
                                and bracket dimensions - which the PARSER threw
                                away, so the read was not in the AST at all.
                                119 -> 27 -> 17 -> 8 -> 6 -> 4 -> 0.
                                warklan 12 agreed/3 missed -> 14/1.
         DONE  #pragma ignore   honoured positionally 2026-08-11: ignore/warn/
                                error by name AND by numeric id, push/pop
                                scoped, flowing through include order. A
                                project silencing a warning in source now
                                silences it here.
         DONE  #warn echo       2026-08-12, and NOT trivial: it needed
                                per-file attribution for walk-time
                                diagnostics, since every preprocessor
                                diagnostic landed at (.dme, line 0) and could
                                never match. Diagnostic gained a File and
                                IncludeGraph backfills it at the file
                                boundary. `#warning` turned out to be a
                                SECOND SPELLING we never mapped, so that whole
                                family fell through as Unknown.
                                WARKLAN NOW MATCHES dm.exe EXACTLY, which
                                puts ALL FOUR projects at 0 missed and
                                0 invented.
       madridspy is now at COMPLETE agreement with dm.exe; warklan 14 -> 3.
       ConstantEvaluator does NOT gate any of this: it gates init_proc, which
       is off by default and so can never be in a missed column.
       Baselines ratchet in tests/fixtures/CORPUS-BASELINE.txt (local only -
       the corpora are games on disk, so CI cannot run it).
    d. DONE 2026-08-11: stddef.dm's 199 #define constants are seeded, and
       the 25 reference-section entries that were never members are gone.

  DONE since the last board: DM0404 (numeric assoc key), ABI 0.21 (per-item
  type and value), the public repo and CI's first run, ABI 0.22 (typeFrom),
  completion after "as", a "." after a written path offering child types,
  DM0302, DM Reference links on builtins, the colour provider (0.23), and
  M8's icon states (0.24).

  CANDY, still unscheduled: .dmm maps, the rung-1 debugger.

  (RESOLVED 2026-08-13: "mob." completing what dm.exe never accepts was
   removed at ABI 0.26, on the user's call. The bare-type-name fallback is
   gone and typed GLOBALS resolve as receivers instead - a gap the fallback
   had been masking. The ~25 non-member scrape entries were fixed
   2026-08-11.)

  STILL THE HIGHEST-RISK UNVERIFIED THING: nobody has watched the VS Code
  client run in a real game. CI cannot cover it.

  1. M11, the live thread. THE BINDER IS IN, with its first two checks:
     DM0400 undefined var and DM0401 undefined proc, on a member reached
     through "." from a receiver whose type is WRITTEN DOWN. mlaas and
     madridspy are at ZERO INVENTED and a control fixture agrees with dm.exe
     on 3 of 3 - the control matters, because a checker that reports nothing
     also scores zero invented.
     Both tree gaps the binder exposed are CLOSED: builtins.txt gained 89
     verified members (/callee had none, /image had one), and a root-level
     type now derives from /datum. The interim guard is gone with them.
     THE TGSTATION RATCHET IS DONE: nine fixes took 225 -> 0 in one day,
     every one a fixture and a regression test. Zero invented is now the
     state to DEFEND - diagdiff on all three projects after any parser or
     preprocessor change.
     THE AGREED LIST IS DONE (user, 2026-08-05 night; all six items):
     signature help on all three surfaces at ABI 0.12 with dmc signature
     as the arbiter; semantic tokens split per line over the
     classification service; $/cancelRequest honoured via a reader
     thread; the dm/objectTree family mirroring abi/schema/; the
     `c ? y : 9 in L` miss, which probing dissolved into a better rule
     (a proc-LOCAL var initializer rejects the relational `in`, with
     locate/input/`in list(...)` exemptions and DM0301 on the last); and
     the duplicate-definition check, DM0403 - same type, any-depth
     ancestor, and builtin conflicts, with the var half recorded as the
     probed remainder. PLAN 8 has all of it.
     NEXT - the agreed order (user, 2026-08-06): working product first,
     error-message parity last, and the net goes in before the big
     change.
       1. DONE: the tier-2 harness (tests/fixtures/services, //? marks
          answered end to end - and it caught a shipped first-character
          off-by-one in definition and hover on its first run) and
          dm_diagnostics at ABI 0.13 (the matrix has no parity gap left).
       2. DONE: THE REFERENCE INDEX at ABI 0.14 - uses with kinds
          (read/write/call/override) and the enclosing symbol per hit,
          from the binder's own walk with a sink, so the index and the
          diagnostics share one resolution. ancestorsOf and dm_invalidate
          rode the same bump (dm-patch's 1, 7 and 15). Surfaces:
          dm_query_json, LSP references/documentHighlight/dm-methods,
          dmc references, tier-2 marks, 140 smoke checks.
       3. M9's tree merge: MEASURED, and the obvious levers are dead
          ends - contribution replay and TypePath hash caching both moved
          nothing, because the cost is the ~335k symbol/dict mutations
          themselves, not the walk. The real lever is diff-patching the
          previous tree, which needs removal semantics; designed as three
          traps in PLAN 6 M9, deferred as its own piece of work. The
          contribution record shipped as its foundation. mlaas stays
          at 10 ms; tgstation ~790 ms.
       4. DONE: $/progress for the first build (create/begin/end; a warm
          tree stays silent), plus the dispatcher now ignores client
          RESPONSES instead of erroring at them. M10 is COMPLETE modulo
          incremental sync, which waits for a profile to ask.
       DONE from that list: hover/definition for MACROS (their 5) — a
       macro name resolves to its #define from any position; project
       macros only, the seeds and -D injects have no source to open.
       Their global-var half was already fixed at HEAD, and the
       builtin remainder is closed too: every builtin hovers from the
       symbol table, and `world` is a root var typed /world.
       DONE: the enclosing-type owner on symbols (their 6) — every
       outline entry carries "owner", resolved with the tree builder's
       own rules, on the ABI and (nonstandard field) the LSP.
       DONE: the bare-. completion context (their 4) — a bare leading
       dot answers ReturnValue with an empty list — and the per-item
       "inferred" flag (their 19), true exactly when the receiver's
       type was worked out rather than written.
       DONE: the readiness signal (their 16) at ABI 0.15 — dm_tree_ready
       reads a field, dm_build_tree warms the tree at open.
       DONE: inlay hints (their 12, the type half) at ABI 0.16 —
       inferred types on untyped locals, the same inference completion
       rides on. Parameter-name hints are the deferred other half.
       DONE: lazy completion documentation (their 11) at ABI 0.17 -
       dm_complete_brief + dm_complete_resolve, LSP resolveProvider.
       MEASURED FIRST, and the measurement matters more than the
       feature: one bare identifier on tgstation offers 19,898 items /
       ~1.0 MB, documentation is 12.7% of it, and full-vs-brief timing
       is inside noise. So it is a payload cut, NOT a speed win, and
       the ITEM COUNT is the real cost.
       DONE: open question 12, answered by the patcher client and
       shipped at ABI 0.18 - completion is RANKED by scope distance
       (locals, params, members, globals, macros, builtins last) and
       reports "truncated"; dm_set_completion_limit is the opt-in cap;
       LSP says isIncomplete and pins order with sortText. The prefix
       filter stays client-side: moving it costs a call per keystroke,
       ~909 ms each on tgstation, against one rebuild per trigger. The
       cap defaults OFF because isIncomplete would cause that same
       per-keystroke re-query. abs fell from ~5th to 19,618th of 19,898.
       From the re-cut UPSTREAM-REQUESTS, still open, near the front:
       parameter-name hints (12's other half), code actions (14,
       unblocked by dm_diagnostics), formatting/folding/links (18).
       NEAR THE FRONT, and RE-CATEGORISED 2026-08-06: dm.exe's own
       DEFAULT-ON WARNINGS on code that COMPILES CLEAN - we miss 14 of
       14 on warklan and 2 of 2 on madridspy. This was sitting in the
       "error-message parity, last" bucket and does not belong there:
       that call was about matching wording on BROKEN code, where the
       compiler has already spoken. These fire on a clean build, so the
       editor is silent about something the build says - a missing
       product feature, not parity polish. unused_var is the likely
       bulk and is already written and BACKED OUT (PLAN 9 has both
       causes). Group the missed lines by warning name before
       estimating; the aggregation axis has lied three times here.
       PLAN 11's baseline table and its new "missed column" section.
       THEN the rest: the probe ratchet's remaining classes (PLAN 8a),
       var duplicate definitions (VarSymbol keeps one site),
       "." on an untyped local, ":" against the
       subtype tree, incremental sync if a profile asks.
       CANDY, unscheduled: .dmm maps, the rung-1 debugger.
       Contract (their do-not-change list): UTF-16 default honest,
       whole-document dm_set_buffer stays, additive ABI bumps minor.
       Parked M11 checks, for after the list above:
       - unused_var: written and BACKED OUT, it invented 13 on mlaas. Two
         causes, one understood (a var block header is not a variable) and
         one not. PLAN 9 has both.
       - "." on an UNTYPED local, which dm.exe rejects for every member
         including the right one. Real, valuable, and needs certainty we saw
         the declaration before it can be safe.
       - ":" against the subtype tree.
  2. M9's tree merge - 411 ms of tgstation's 909, and the last phase still
     rebuilding the whole project from scratch.
  3. SEND tests/abi-smoke TO THE IDE DEVS. The CMakeLists include and link
     lines are meant to be copied verbatim.
  4. Nothing about the language is a guess now. Every open question left is
     about the project rather than about DM.


MILESTONES                                    (restructured 2026-08-02)
--------------------------------------------------------------------------------
  [x] M0   Boundary + project setup
             C# -> NativeAOT -> C ABI -> C++ proven before any compiler code,
             because the ABI was the riskiest infrastructure. CI included.
  [x] M1   Text layer + lexer
             clean across 279 files / 303,015 tokens of real DM.
  [x] M2   Lexical classification
             first visible feature. moved here from M9/M10 - see WHAT CHANGED.
  [x] M3   Preprocessor + include graph
             encoding detection, conditionals, macro expansion, source maps.
  [x] M4   Parser + syntax errors + document symbols
             declarations, expressions and statements all parse; proc bodies
             are real trees. reads the PREPROCESSED stream by default.
  [x] M5   Object tree + BYOND builtins
             1,018 entries for 516.1666 bundled, including stddef's 199
             #define constants. A typed receiver resolves loc and Move().
  [x] M6   Binder + completion
             "mob." and "t." work. type inference through new/as/assignment,
             which is the ONE place we knowingly disagree with dm.exe.
  [x] M7   Workspace symbols + navigation + bulk queries    <- TEAM V1
             definition (0.6), hover (0.7), wsymbols (0.8), query_json (0.11).
  [x] M8   .dmi icon states                          <- DONE 2026-08-08
             Passed over at the M8/M9 fork and picked up five milestones
             later. Dm.Assets + DmiReader, dm_icon_states at ABI 0.24, LSP
             dm/iconStates, dmc icons. Built against 352 REAL icons from
             three games, which is what set the contract: zero-byte .dmi
             files exist, one name can appear twice (movement), and an
             empty state name is the default rather than an error.
             Python and dmc agree exactly on all 4,186 states.
  [x] M9   Incremental reparse + performance    <- TARGET MET, 13x/15x
             five levers, each measured before and after. 1 of 7,161 files is
             re-read, re-walked and re-parsed after a keystroke.
             REMAINING: the tree merge, 411 ms of 909.
  [x] M10  LSP server + VS Code extension       <- community v1, DONE
             (incremental document sync deliberately waits for a profile
             to ask - full sync costs one string against a ~10 ms rebuild)
             Dm.Lsp serves initialize, sync, publishDiagnostics (parse +
             binder), completion, hover, definition, symbols over stdio -
             hand-rolled JSON-RPC, no dependencies, which resolved open
             question 6. editors/vscode has the client: npm install, F5,
             open your game. Signature help, semantic tokens, cancellation
             and the dm/* bulk methods all ship. REMAINING: $/progress,
             incremental sync if a profile asks.
  [~] M11  Semantic diagnostics, find-refs, rename    <- STARTED
             harness first, code second. one check landed early as DM0300,
             because the parser had to model the construct to match dm.exe
             and knowing the author wrote it is then free.
             THE BINDER IS IN: DM0400 undefined var, DM0401 undefined proc.
             Built against the oracle from its first run, which caught 14
             invented diagnostics across two clean projects before any of
             them could be believed.

  WHAT CHANGED AND WHY
    Syntax highlighting was at M9/M10 as a TextMate grammar plus LSP semantic
    tokens. Neither does anything for an FNA/C# window or a Qt IDE. What those
    need is a classify-this-range call returning coloured spans, and that needs
    the LEXER ONLY - so it moved to M2, and first visible payoff moved from M6
    to M2. Document symbols moved to M4 for the same reason: a per-file outline
    needs the AST, not the object tree.


WHY DM IS HARD                        (one line each; the detail is in PLAN.md)
--------------------------------------------------------------------------------
  Dots and slashes         mid-path "." and "/" are the SAME token, "proc" and
                           "verb" are ordinary segments, and a LEADING "." is a
                           search rather than a traversal.            PLAN 4a
  Include order is semantic  overrides resolve by .dme order, so editing a
                           #define invalidates everything downstream. And
                           "var.thing.T" parses differently depending on
                           whether /thing was declared yet - swapping two
                           #include lines flips it.                   PLAN 4a
  Two block syntaxes       braces AND significant indentation, mixed in one
                           file, nesting freely in either order.      PLAN 8
  Builtins are invisible   mob.Move(), .loc, .client appear in no user code and
                           must be scraped from the BYOND reference.  PLAN 6/M5
  Macro-heavy reality      without source maps every completion and error lands
                           on the wrong line.                         PLAN 6/M3
  Error recovery           editor buffers are broken on every keystroke.
  ":" is not unchecked     it WIDENS the check to subtypes. filtering it to
                           nothing, or to everything, are both wrong.  PLAN 4a
  Nesting block comments   DM nests /* */, unlike C.                   PLAN 8
  Odd but legal forms      comma var lists, ";" separators, single-line proc
                           bodies, default params, bare inherited-var
                           assignment, operator overloads incl. operator:=
                           (which contains a colon).                   PLAN 4c
  Position encoding        LSP wants UTF-16, the C ABI wants UTF-8 bytes.
                           Support both or retrofit every signature.   PLAN 4
  NativeAOT constraints    no reflection or dynamic in Dm.Core, no exception
                           may cross the boundary, per-platform binaries.
                           PLAN 7
  We become the platform   two IDEs depend on our ABI. Breaking changes cost
                           other people real work.


YOUR CORPUS - RUN scan AFTER ANY LEXER CHANGE
--------------------------------------------------------------------------------
    ~\Desktop\world                      stddef.dm + your experiments
    ~\Documents\GitHub\dm-bench
    ~\Documents\GitHub\madridspy
    ~\Documents\GitHub\mlaas             the correctness harness
    ~\Documents\GitHub\warklan
    ~\Documents\BYOND\lib                third-party libs your code #includes
    W:\new-reorg\projects\games\byond    2,269 files / 2.47M tokens

  Clean everywhere. The only files still flagged are 8 in the W: archive that
  are PROSE saved with a .dm extension - story text, design notes, one pasted
  chat log. dm.exe rejects them too, so flagging them is correct:

    a-game-like-redcap\Story.dm             mspy\...\rules.dm
    mlaas-ideas\mlaas ideas.dm              old-spies-byond\...\rules.dm
    alien-warfare\with-4.0\summary.dm       spies-prehistory\...\Skills.dm  (x2)
                                            spies-prehistory\...\stuff.dm

  NOTE: scan globs *.dm off disk and does NOT follow the include graph, so it
  misses library code behind "#include <vendor/name>". That is why the BYOND
  lib path is listed separately. Use "dmc includes" / "dmc preprocess" for the
  real graph.

  Real code beats synthetic tests here every time. Five lexer bugs came out of
  these repos that no amount of test-writing would have produced - see PLAN 6.
  The W: archive alone went 130 unknown tokens / 17 problem files -> 19 / 8,
  and 113 of those 130 were ONE construct: "\" escapes in names.


NOTED IN YOUR PROJECT (mlaas) - not our bug, might be yours
--------------------------------------------------------------------------------
  src\_constants.dm is included TWICE (spies.dme lines 7 and 9). Harmless, the
  compiler ignores the repeat. Line 9 is inside DreamMaker's auto-generated
  block, so line 7 is the redundant one.

  8 .dm files on disk are NOT reached by spies.dme, so they are dead unless
  something else pulls them in:
      src\lighting\backdrop.dm        src\lighting\lighting_sources.dm
      src\lighting\core.dm            src\lighting\planes.dm
      src\lighting\daynight.dm        src\lighting\sun.dm
      src\lighting\light.dm           src\white\white_training.dm
  The .dme's line 3 has a commented-out sd_DynamicAreaLighting include, so the
  lighting set looks like a deliberately disabled subsystem.

  In madridspy, four mission procs are declared inside a var block and DO NOT
  EXIST at runtime, and code\M_atoms other_obj.dm:286 calls one of them. One
  dedent fixes it. That is what DM0300 warns about - PLAN M11.


WHERE THE DETAIL LIVES
--------------------------------------------------------------------------------
  Read this table instead of grepping four files. Each fact has ONE home.

    the design, milestones, the ABI contract          PLAN.md
    DM path semantics, "." vs ":", leading "."        PLAN.md 4a
    line endings, and why they are the client's job   PLAN.md 4b + INTEGRATION 5
    operator precedence, overloadable operators       PLAN.md 4c
    every language fact verified against dm.exe       PLAN.md 8
    dm.exe flags, the three oracles, warning names    PLAN.md 8
    PERFORMANCE: the lever-by-lever keystroke table
      (131 -> 87 -> 41 -> 21 -> 14 -> 10 ms), the phase
      breakdown, and what each cache bought            PLAN.md 6, M9
    recall tables, the oracles, the -o blind spot      PLAN.md 9
    per-lever write-ups, traps, what is left           docs/internal/state.md
    open questions                                    PLAN.md 10

    how to CALL any of it, ownership, encodings,
      troubleshooting by symptom, what an edit costs  INTEGRATION.txt
    the LSP: launch, initializationOptions, the
      dm/* methods, what is NOT served, Neovim and
      Helix config                                    docs/lsp.md
    runnable proofs of 18 DM edge cases               docs/dm-language-notes.md
    the fixture suite: compile + RUN + diagdiff       tests/fixtures/README.md
      - and what to do when BYOND updates             tests/fixtures/README.md
    the compiler's own diagnostic catalogue,
      30 warning ids with their pragma names          PLAN.md 8a
    working notes, verified commands, dead ends       docs/internal/  (ignored)

  THE RULE THAT KEEPS THIS TRUE: the doc edit belongs in the commit that makes
  the old text wrong, not in a later cleanup pass. Three cleanup passes have now
  been needed, and each found a live doc contradicting itself.


OPEN QUESTIONS
--------------------------------------------------------------------------------
  None. The last two were dropped 2026-08-13 by the user: the third IDE's dev
  is not active, and nothing SpacemanDMM-related will be borrowed, so neither
  question has a consumer. Full table with evidence in PLAN.md 10.


CONVENTIONS
--------------------------------------------------------------------------------
  - PLAN.md, ROADMAP.txt, INTEGRATION.txt and docs/dm-language-notes.md are all
    LIVE docs: they describe now, at every commit.
  - docs/internal/ is Claude's working notes. Gitignored. Not team-facing.
  - EVERY NEW FINDING GETS A FIXTURE CASE, in the same change. tests/fixtures
    is the regression net; anything left out of it gets re-learned. A corpus of
    correct code cannot tell us what we wrongly ACCEPT - dm.exe reports zero
    diagnostics on tgstation's 1.5M lines - so the must-fail and must-mean-this
    cases only exist if someone writes them.
  - Commits: concise and factual. Group related changes; don't split per line,
    don't lump unrelated work together. No AI attribution lines.
  - Prose: no hype, no "it's not X, it's Y", fact-driven.
