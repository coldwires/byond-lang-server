================================================================================
 BYOND DM LANGUAGE LIBRARY - INTEGRATION GUIDE
 For anyone wiring this into an editor or IDE.

 LIVE DOCUMENT. Updated as features land. If something here is wrong, it is a
 bug in the doc - say so.

 ABI version at last update: 0.29  (39 exports, cdecl, six RIDs)
 Last updated: 2026-08-15
================================================================================


WHAT THIS IS
--------------------------------------------------------------------------------
  A library that reads DM (DreamMaker) source and answers questions about it.
  You embed it. It has no UI and no opinions about your editor.

  It ships as a normal native shared library with a plain C header. You do not
  need .NET installed, and you do not need to know it is written in C#.

    Windows   dm_core.dll  + dm_core.lib
    Linux     dm_core.so
    macOS     dm_core.dylib

  Anything with a C FFI can call it. C++, C, Rust, Python, Go, Zig, C#.


WHAT YOU GET TODAY
--------------------------------------------------------------------------------
  WORKING NOW
    - Syntax highlighting (lexical). Keywords, strings, comments, numbers,
      operators, preprocessor directives, resource literals.
    - Correct handling of the DM constructs that break naive highlighters:
      nested /* */ comments, {" multiline "} strings, "[interpolation]" holes,
      and // inside a path eating the rest of the line.
    - Unsaved buffer support. You push editor text, we analyse that, not disk.
    - MACROS ARE EXPANDED. The object tree is built from the preprocessed
      stream, so a type or var declared through a macro is the declaration
      it expands to rather than the macro's name. Codebases that declare
      through macros - which is most large ones - now resolve.
    - Autocomplete via dm_complete_at. Locals, parameters, src members,
      inherited members and BYOND builtins, with the . / : distinction the
      compiler makes. A receiver resolves when its type is written down -
      src, a typed local, a typed parameter, a literal path - and otherwise
      by inference from new / as / assignment, with "typeFrom" naming which
      route it was. After `as`, the eighteen input filters a verb argument
      accepts; after a `.` that continues a written path, child types rather
      than members. See the note in section 4.
    - Go to definition via dm_definition_at. Type paths, members through
      their receiver, bare names, and macros to their #define. Returns
      EVERY declaration - see section 4, this is the part most likely to
      surprise you.
    - Workspace symbol search via dm_workspace_symbols. Ranked and capped,
      so a two-character query stays usable.
    - Hover via dm_hover_at. The resolved path, the declaration as written,
      and any doc comment above it - both /// runs and /** */ blocks.
      Macros hover as their #define line, doc comment included; builtins
      hover with a signature rendered from the symbol table, `world`
      included, plus a DM Reference link for the ones the reference
      documents.
    - Signature help via dm_signature_at. Which proc an open argument list
      belongs to and which parameter the caret is in, exact mid-keystroke.
    - Rename via dm_rename_at. Best-effort with the uncertainty REPORTED:
      provable edits, plus the `:`/untyped/string sites a human must check.
    - Semantic highlighting. Colour IDs 12-15 distinguish a type name, a
      proc name, a var name and a macro name. See COLOURS below.
    - Document outline / symbols, with syntax diagnostics, via
      dm_document_symbols. Types, vars, procs and verbs, nested, each with a
      range for the whole declaration, a second range for the name alone,
      and the resolved path of what owns it.
    - Object tree browsing via dm_query_json. A node and its children to a
      depth, every subtype of a path, one type's members with inheritance
      resolved, or the whole ancestor chain in one call (ancestorsOf). This
      is what a tree panel runs on. Request and response shapes are frozen
      in abi/schema/.
    - THE REFERENCE INDEX, via dm_query_json's "references" query: every USE
      of a var, proc or type across the project, each hit carrying kind
      (read | write | call | override) and the enclosing symbol - group by
      it and you have a call hierarchy. Found by the same resolution the
      diagnostics use, so the index and the squiggles cannot disagree.
    - dm_invalidate, for files changing OUTSIDE the editor - a git checkout,
      a branch switch. Drops derived answers; the rebuild revalidates
      per-file, so only what actually changed is reprocessed.
    - THE READINESS SIGNAL: dm_tree_ready says whether the object tree
      exists right now, and dm_build_tree warms it at a moment you choose
      - so "indexing..." is a fact you read, not a guess from latency.
    - INLAY HINTS via dm_inlay_hints: the inferred type of every untyped
      local, rendered after its name - var/x = new /obj/item gains
      ": /obj/item" - and the name of the parameter each argument fills,
      rendered before it: heal(amount: 5). The type is exactly what a
      reader does not have, because dm.exe never checks it and nothing
      forces the author to write it.

    - Semantic error squiggles via dm_diagnostics: undefined vars and procs
      through a typed receiver, duplicate definitions, the DM03xx
      compiles-clean-but-lies warnings, and three of dm.exe's own default-on
      warnings carrying its names - new_name, no_parent and unused_var. The same set the
      LSP publishes and dmc diagdiff holds at ZERO INVENTED against dm.exe on
      1.5M lines. `#pragma ignore` is obeyed; see section 4.
    - The editor-shaped surfaces: dm_type_definition_at (where a var's
      WRITTEN type is declared), dm_folding_ranges (from the AST, since
      DM's two block syntaxes nest freely and indentation-based folding
      drops everything inside braces) and dm_document_links (clickable
      #include targets, resolved off the token stream so they work before
      the project has been walked).
    - COLOUR SWATCHES via dm_document_colors: every "#rrggbb" literal and
      rgb() call in a file, with the text to write back if the user picks a
      new shade. Needs no object tree, so it answers immediately.
    - dm_file_in_project: whether the include walk actually reaches a
      file. A buffer you push for a file the .dme never includes analyses
      fine per-file while its own declarations resolve nowhere, which is
      indistinguishable from a broken push - this is how you tell.
    - STANDALONE ANALYSIS via dm_workspace_open_standalone: a project with
      no .dme at all, or a scratch file, resolves against the builtins
      plus itself. It deliberately cannot see another file's declarations,
      because the compiler could not either.
    - THE .dme TICKMARKS via dm_dme_is_ticked / dm_dme_tick /
      dm_dme_untick: read and edit DreamMaker's own // BEGIN_INCLUDE
      block, returned as a span plus replacement rather than written to
      disk - the .dme is usually open and dirty in the editor that asked.

    - ICON STATES via dm_icon_states: every state in a .dmi, with its dirs,
      frame count, delays and animation flags. This is what an icon browser
      and an icon_state autocomplete run on.

  NOT YET - do not design around these being absent, they are coming
    - .dmm map support, and a debug adapter. Both unscheduled; say so if
      either would change your plans.

  Syntax errors arrive two ways: beside the outline in dm_document_symbols,
  and - together with the semantic set - from dm_diagnostics, which is the
  call to use when you want squiggles without paying for the outline.


--------------------------------------------------------------------------------
 0. BUILD IT
--------------------------------------------------------------------------------
  You need the .NET 9 SDK, and on Windows the MSVC C++ build tools.

    dotnet publish src/Dm.Native/Dm.Native.csproj -c Release -r win-x64

  Valid RIDs: win-x64, win-x86, linux-x64, linux-arm64, osx-x64, osx-arm64.

  ** CALLING CONVENTION IS cdecl ON EVERY PLATFORM **
  Declared explicitly on the exports rather than left to the default. On x64
  there is only one convention and this never comes up; on 32-bit Windows
  NativeAOT would otherwise emit stdcall, which C, Rust, Go, Zig and Python's
  ctypes all assume otherwise. Include dm_core.h and call - no annotation, no
  WINAPI, no extern "stdcall". If you hand-write declarations instead, cdecl is
  what to write, and getting it wrong on x86 corrupts the stack rather than
  failing to link.

  win-x86 exists for hosts that are themselves 32-bit. It is in CI and the C++
  smoke test runs there, not merely links: cmake -A Win32 -DDM_RID=win-x86.

  ** WINDOWS GOTCHA **
  If that fails with MSB3073 and something about link.exe, the real cause is
  that vswhere.exe is not on PATH. The error message blames the linker and is
  lying to you. Fix:

    $env:PATH = "C:\Program Files (x86)\Microsoft Visual Studio\Installer;$env:PATH"

  Output lands in:
    src/Dm.Native/bin/Release/net9.0/<rid>/publish/   <- the .dll / .so
    src/Dm.Native/bin/Release/net9.0/<rid>/native/    <- the .lib (Windows)


--------------------------------------------------------------------------------
 1. FILES YOU NEED
--------------------------------------------------------------------------------
    abi/dm_core.h        the header. this is the source of truth, read it.
    dm_core.dll          ship next to your executable
    dm_core.lib          link against this (Windows only, it is an import lib)

  There is a working CMake project at tests/abi-smoke, current with ABI 0.29 and
  passing 260 checks on x64 AND x86. It covers version negotiation, string
  ownership, handle lifetime including use-after-close, classification in both
  encodings, document symbols, completion, injected defines, go-to-definition,
  hover, signature help, diagnostics, the reference index, invalidation, the
  readiness signal, workspace symbols, the editor surfaces, standalone
  analysis and the .dme tickmarks. The include and link lines in its CMakeLists.txt are meant
  to be copied verbatim. If you are stuck on build setup, build that first and
  confirm it runs - it is 30 seconds and it separates "my build is wrong" from
  "my code is wrong".


--------------------------------------------------------------------------------
 2. LINK IT
--------------------------------------------------------------------------------
  CMake, Windows:

    target_include_directories(myapp PRIVATE "path/to/abi")
    target_link_libraries(myapp PRIVATE "path/to/native/dm_core.lib")

    add_custom_command(TARGET myapp POST_BUILD
      COMMAND ${CMAKE_COMMAND} -E copy_if_different
              "path/to/publish/dm_core.dll" "$<TARGET_FILE_DIR:myapp>")

  Linux and macOS:

    add_library(dm_core SHARED IMPORTED)
    set_target_properties(dm_core PROPERTIES
      IMPORTED_LOCATION "path/to/publish/dm_core.so")   # .dylib on macOS

    target_link_libraries(myapp PRIVATE dm_core)
    set_target_properties(myapp PROPERTIES BUILD_RPATH "path/to/publish")

  ** THE IMPORTED TARGET IS NOT CEREMONY, IT IS REQUIRED **
  NativeAOT names these dm_core.so and dm_core.dylib, with NO `lib` prefix, and
  CMake only treats a full path as a library FILE when the name matches the
  platform convention (libfoo.so). Given dm_core.so it decides you meant a link
  ITEM NAME instead and emits -ldm_core, so the link dies with

    /usr/bin/ld: cannot find -ldm_core

  while the file is sitting exactly where you pointed it. The obvious one-liner
  target_link_libraries(myapp PRIVATE "path/to/publish/dm_core.so") is the form
  that fails. This guide recommended it until CI first ran a Linux build.

  BUILD_RPATH is separate and also required: it is how the loader finds the
  library at RUN time, and the publish directory is on no default search path.

  ** macOS NEEDS ONE MORE STEP, BEFORE YOU LINK **
  NativeAOT bakes a RELATIVE install name into dm_core.dylib, something like
  bin/Release/net9.0/osx-arm64/native/dm_core.dylib. macOS copies that string
  into your executable AT LINK TIME as the dependency to load, so your program
  builds and then dies at startup:

    dyld: Library not loaded: bin/Release/net9.0/osx-arm64/native/dm_core.dylib

  An rpath does not rescue it, because the recorded name is not @rpath/... .
  Rewrite the install name first, then link:

    install_name_tool -id "@rpath/dm_core.dylib" path/to/publish/dm_core.dylib

  It has to happen BEFORE the link. Afterwards the old string is already inside
  your executable and you would be fixing it there instead, one binary at a
  time. tests/abi-smoke/CMakeLists.txt does this at configure time.

  The header already has extern "C" guards. Just #include it from C++.


--------------------------------------------------------------------------------
 3. THE MINIMUM PROGRAM
--------------------------------------------------------------------------------
  This is the whole thing. Everything else is detail.

    #include "dm_core.h"

    // 1. check we agree on the ABI
    if (DM_ABI_VERSION_MAJOR(dm_abi_version()) != DM_ABI_EXPECTED_MAJOR)
        fatal("dm_core is a different major version than this build expects");

    // 2. open the project
    dm_workspace ws = NULL;
    if (dm_workspace_open("C:/games/mygame/mygame.dme", &ws) != DM_OK) {
        char *msg = dm_last_error();
        fatal(msg);
        dm_free(msg);
    }

    // 3. tell us what the build defines, before anything else asks a question
    const char *defines[] = { "CBT" };
    dm_set_defines(ws, defines, 1);

    // 4. every time the user types, push the buffer
    dm_set_buffer(ws, "code/mob.dm", utf8_text, utf8_byte_length);

    // 5. every time you paint, ask for the visible lines
    dm_classification spans = NULL;
    dm_classify_range(ws, "code/mob.dm", first_visible_line, last_visible_line,
                      DM_ENCODING_UTF16, &spans);

    int32_t n = dm_classification_count(spans);
    const int32_t *d = dm_classification_data(spans);

    for (int32_t i = 0; i < n; ++i) {
        int32_t offset = d[i * 3 + 0];
        int32_t length = d[i * 3 + 1];
        int32_t kind   = d[i * 3 + 2];
        paint(offset, length, colour_for(kind));
    }

    dm_classification_free(spans);

    // 6. at shutdown
    dm_workspace_close(ws);


--------------------------------------------------------------------------------
 4. THE CALLS, IN ORDER
--------------------------------------------------------------------------------
  dm_abi_version()
      Call first, once. Returns (major << 16) | minor. Use the
      DM_ABI_VERSION_MAJOR / _MINOR macros. Refuse to run on a major mismatch.
      A higher minor than you were built against is fine - additions only.

  dm_workspace_open(dme_path, &ws)
      Opens a project. This validates the .dme exists and records its
      directory; it does no parsing. The include graph is walked and the object
      tree is built on the first call that needs them - in practice
      dm_complete_at - and is then cached until a buffer changes.
      Returns DM_ERR_NOT_FOUND if the .dme is not there.

  dm_workspace_open_standalone(root_dir, &ws)                NEW in 0.20
      Opens a workspace with NO .dme, rooted at a directory. Every file
      is then analysed as its own single-file project: the builtins plus
      itself.

      This is what to call when the user opens a folder that has no .dme,
      or a loose .dm file. The alternative - refusing to open - meant an
      editor that showed nothing at all, when per-file answers were
      available the whole time.

      ** IT CANNOT SEE ANOTHER FILE'S DECLARATIONS, AND THAT IS CORRECT **
      Nothing has told us those files are one program. dm.exe compiling
      the same file alone would resolve exactly as much. So a standalone
      file completes its own procs and vars and the builtins, and nothing
      from the folder beside it.

      A workspace opened this way behaves identically everywhere else:
      buffers, defines, and every position-shaped call work as documented.

  dm_set_defines(ws, defines, count)                          NEW in 0.5
      Defines macros for the project, exactly as dm.exe's -D switch does.

      ** PASS WHAT THE PROJECT'S BUILD PASSES **
      The flags decide which #ifdef branches exist, so a workspace without
      them describes a DIFFERENT PROGRAM from the one the build produces:
      code behind a guard is invisible to you, or visible when it should not
      be. /tg/station, for example, builds with -DCBT.

      Spelling is the compiler's own:
        "NAME"              defined EMPTY, not 1 - matches dm.exe
        "NAME=value"
        "FN(x)=((x)*2)"     function-like

      Call it straight after opening. The object tree is lazy, so it still
      applies to your first query, and you can change flags later without
      reopening. NULL or count 0 clears them. Strings are copied.

      If your users configure build flags anywhere, plumb them through to
      this call - it is the difference between analysing their program and
      analysing a similar one.

  dm_set_buffer(ws, file, content, length)
      Tells us what the user currently has on screen for that file.

      ** ONCE YOU CALL THIS, WE NEVER READ THAT FILE FROM DISK AGAIN **
      (until dm_close_buffer). That is deliberate. Your editor is ahead of the
      filesystem on every unsaved keystroke, and disk is the wrong answer.

      `content` is UTF-8 and is copied before the call returns - you can free
      your copy immediately. `length` is in BYTES. Pass -1 if it is
      null-terminated, but prefer the real length: it avoids a scan, and DM
      string literals can legally contain a NUL.

      `file` may be absolute, or relative to the folder holding the .dme.

      Call this on every edit. It is cheap - it stores text and marks derived
      data stale. Nothing is re-analysed until you ask a question.

  dm_close_buffer(ws, file)
      User closed the tab. We go back to reading that path from disk.

  dm_invalidate(ws)                                             NEW in 0.14
      Files changed OUTSIDE the editor: a git checkout, a branch switch,
      another program saving. Drops every derived answer so the next
      question rebuilds against current disk. Cheap by construction - the
      per-file caches revalidate by write time and length during the
      rebuild, so only files that actually changed are reprocessed. Pushed
      buffers stay authoritative. If your host can watch the filesystem,
      call this when the watcher fires; if not, calling it on window focus
      is the pragmatic version.

  dm_tree_ready(ws)                                             NEW in 0.15
      Whether the object tree exists RIGHT NOW: 1 built, 0 not, -1 invalid
      handle. Costs nothing - it reads a field - so poll it freely. This
      is how you show "indexing..." instead of a frozen UI: 0 means the
      next tree-backed call pays for a build, and the signal drops back to
      0 whenever a buffer, a define change or dm_invalidate drops the
      tree. Stops you inferring readiness from a call taking a long time.

  dm_build_tree(ws)                                             NEW in 0.15
      Builds the object tree now, blocking until it exists. The
      warm-at-open call: pay the cold cost at a moment of your choosing -
      a splash screen, a background thread at startup - instead of on the
      user's first completion. A warm tree makes it a no-op, so calling it
      defensively is free. The threading contract is unchanged: one
      workspace, one thread at a time, so warm on your analysis thread.

  dm_classify_range(ws, file, start_line, end_line, encoding, &out)
      Line numbers are ZERO-BASED and INCLUSIVE. Out of range values clamp,
      they do not error.
      Ask only for what is visible. Asking for the whole file works but is
      wasted effort on a 10,000 line file where you are showing 60 lines.

  dm_document_symbols(ws, file, encoding, &json)
      The file's outline plus its syntax diagnostics, as one UTF-8 JSON
      document. YOU FREE IT with dm_free.

      Why JSON when classification is a packed block: symbols carry names, and
      a packed int32 block cannot express a string without a table on both
      sides. An outline is rebuilt when the file changes, not on every paint,
      so the trade goes the other way here.

      Each symbol has TWO ranges, and the difference matters:
        startLine/startChar .. endLine/endChar    the whole declaration,
                                                  members included
        selStartLine/selStartChar .. selEnd*      the NAME only

      Navigate with the sel* range. Clicking an outline entry then puts the
      caret on the identifier rather than selecting the entire block.

      kind is 0 type, 1 variable, 2 proc, 3 verb, 4 parameter. Treat an
      unknown kind as a neutral icon so a library update cannot break you.

      "owner" is the resolved path of whatever CONTAINS the symbol: a
      member's owning type ("/mob"), a type's parent by path, a
      parameter's proc spelled as the reference index spells "inside"
      ("/mob/heal()"). "/" for anything at the root. This is the field a
      hierarchy view or breadcrumb wants - resolved with the same owner
      rules the object tree uses, so a one-line /mob/TEA() puts TEA on
      /mob and a mob/proc group header carries its path. Do not
      reconstruct it by slicing other fields.

      Diagnostics in the same document are the syntax errors for that file -
      enough for squiggles. The SEMANTIC set arrives through dm_diagnostics,
      below, which also repeats the syntax errors so one call carries
      everything a squiggle pass needs.

      ** ONE WARNING ALREADY ARRIVES HERE: DM0300 **
      A proc block indented one level too far, into a var block, declares
      nothing at all - dm.exe compiles it with 0 errors and silently drops
      every proc under it, so calling one is a runtime error on a code path
      with a clean build. We match the compiler and declare nothing, which
      means those procs are NOT in the outline, and we warn on the header,
      which the compiler never does.

      Severity is warning, not error: the file does compile. DM03xx is that
      class - code that compiles and does not mean what it looks like -
      and more of them arrive at M11. Render them differently from the
      syntax errors if you can; this one is usually a real bug in the game.

  dm_diagnostics(ws, file, encoding, &json)                     NEW in 0.13
      Every diagnostic for one file - syntax AND semantic - as UTF-8 JSON.
      YOU FREE IT with dm_free.

      This is squiggles without the outline, and the only call carrying the
      semantic set: undefined vars and procs reached through a typed
      receiver, duplicate definitions, and the DM03xx warnings for code that
      compiles clean and does not mean what it looks like. It is the same
      set dmc diagdiff measures against dm.exe and holds at zero invented,
      so a diagnostic from this call on a project that builds clean is a
      bug worth reporting.

      Elements are byte-identical to dm_document_symbols' diagnostics:
      id, severity (a WORD - "error", "warning"), message, and a zero-based
      range in your encoding. An empty array with DM_OK is the ordinary
      answer on a clean file.

      ** WE OBEY #pragma ignore **
      Some of what we report carries dm.exe's OWN warning name rather than a
      DM0xxx id - "new_name", "no_parent", "unused_var" - and those are the names
      `#pragma warn|ignore|error` takes. A project that writes

          #pragma ignore no_parent

      has told the compiler not to say it, and we treat that as having been
      told too. Numeric ids work as well as names (`#pragma ignore 3013`),
      `#pragma push`/`pop` scope it to a region, and the level flows through
      INCLUDE ORDER - so an ignore in the first file a .dme includes silences
      the rest of the project, and swapping two #include lines changes the
      answer. All of that is the compiler's behaviour, verified against it.

      The consequence for you: a diagnostic disappearing after an edit to some
      other file may be a pragma rather than a bug. `dmc diagdiff` shows the
      same set we do.

      Our own DM0xxx ids have no pragma name and are never suppressed this
      way - they are the things dm.exe has no name for.

      ** COST ** The semantic half needs the object tree, so the first call
      after an edit rebuilds it - same as dm_complete_at. Debounce it.

  dm_complete_brief(ws, file, line, character, encoding, &json)    NEW in 0.17
  dm_complete_resolve(ws, file, line, character, name, encoding, &json)
      The same completion list with every "documentation" left empty, and a
      second call that fills in ONE item's when the user highlights it.
      YOU FREE both with dm_free.

      Resolve takes the file and position you completed at plus the item's
      "name", and answers { "documentation": "..." }. Stateless: nothing is
      retained between the two calls, so there is no handle to go stale and
      no ordering to get wrong. DM has no overloads, so a name at a position
      is unambiguous. An empty string is normal - most symbols have no doc
      comment.

      ** WHAT THIS BUYS, AND WHAT IT DOES NOT **
      Measured on /tg/station, where one bare identifier offers 19,898
      items totalling ~1.0 MB: documentation is 12.7% of that. So brief
      cuts the bytes you marshal, and that is the whole of it. It is NOT
      faster - we time full against brief inside run-to-run noise, because
      the doc lookups run over text already cached.

      If you were hoping for speed, the honest answer is that the ITEM
      COUNT is the cost and neither of these changes it. We would rather
      say so than let you adopt a call that does not solve your problem.

  dm_inlay_hints(ws, file, start_line, end_line, encoding, &json)  NEW in 0.16
      The inferred type of every untyped local in a line range, as UTF-8
      JSON. YOU FREE IT with dm_free. Lines are ZERO-BASED and INCLUSIVE,
      same as dm_classify_range; ask for what is visible.

      Each hint: line, char (in your encoding), label (render as
      written), and kind - treat an unknown kind as opaque, more arrive
      later. TWO kinds ship today, and the char means something
      different in each:

        "type"       an untyped local's inferred type, AFTER the name:
                     var/x = new /obj/item  ->  ": /obj/item"
        "parameter"  the name of the parameter an argument fills,
                     BEFORE the argument:  heal(amount: 5)

      A local with a WRITTEN type gets no hint; every type hint is
      inference dm.exe does not do, shown through the same machinery
      completion rides on, so the hint and the completion list cannot
      disagree about what a name holds. A parameter hint is suppressed
      where the source already says it - `f(name = value)`, and an
      argument whose own text is the parameter's name.

      ** THE PARAMETER KIND WAS "unknown" UNTIL 0.29 **
      The hints themselves have been in the answer since 2026-08-12; the
      writer had no case for them and fell through to "unknown", which
      this guide tells you to treat as opaque. If you pinned an earlier
      minor and dropped unknown kinds, you were dropping parameter
      hints. Nothing else about them changed.

      ** COST ** The first call after an edit builds the object tree.
      Re-ask on scroll and after edits, not per keystroke.

  dm_complete_at(ws, file, line, character, encoding, &json)
      What can be typed at a position, as UTF-8 JSON. YOU FREE IT with
      dm_free. Line and character are ZERO-BASED.

      Point it at the caret. A half-typed word is not the trigger - whatever
      precedes it is - so "mob.he" completes against /mob exactly as "mob."
      does. Filter the list by the typed prefix yourself.

      "context" tells you why the list is what it is:
        Identifier      a bare word: locals, params, src members, globals
        Member          after `.` - the declared type and what it inherits
        SubtypeMember   after `:` - the above PLUS members from subtypes
        TypePath        after `/`, and after a `.` CONTINUING A WRITTEN PATH -
                        child types, not members. Mid-path `.` and `/` are the
                        same token, so `/obj/item.weight` is the type
                        `/obj/item/weight` and dm.exe says "undefined type
                        path" when no such child exists. We offered members
                        there until 2026-08-07, which is a completion that
                        cannot build. Same after `new /mob/test.`
        InputType       after `as`, and after a `|` continuing one - the
                        eighteen input filters a verb argument accepts. A
                        CLOSED vocabulary, compiled one candidate at a time
                        rather than derived: `as datum`, `as list` and
                        `as client` are rejected by the compiler while
                        `as movable` and `as atom` are accepted, which no rule
                        about types predicts. Items are marked builtin and
                        carry the detail "input filter"
        IconState       inside an `icon_state = "..."` string - the states of
                        the .dmi THAT TYPE ACTUALLY USES. Found by resolving
                        `icon` through the inheritance chain, so a subtype that
                        sets only icon_state still offers its parent's icon,
                        which is how DM is written. Items carry kind "value"
                        and the icon's path as their detail.
                        The EMPTY name is the default state and is completely
                        ordinary - 226 of 352 real icons have one - so it
                        completes to an empty string rather than being hidden.
                        An empty list with this context means we could not
                        resolve an icon, which is different from there being
                        no states.
        ReturnValue     a bare leading `.` - DM's return-value variable,
                        not member access. The list is empty; show nothing.
                        This is the context that saves you guessing what a
                        user who just typed `.` on a fresh line wanted.
        None            nothing useful here

      Treat an unknown context word as Identifier and show the list as given.
      More arrive as the parser learns positions, and a client that switches
      exhaustively on this breaks on a minor bump.

      ** THE LIST IS RANKED, SO KEEP OUR ORDER **                NEW in 0.18
      Ordered by scope distance, nearest first: locals, parameters, the
      enclosing type's members, globals, macros, and BYOND's builtins
      last. Alphabetical order put abs() above a local declared two lines
      up. The ranking a search box uses - exact, then prefix, then
      substring - cannot help at a bare identifier, because there is no
      query string to rank against; scope distance is what the position
      does know. Nothing in the response repeats this as a number: the
      order IS the ranking, so preserve it. (The LSP writes it into
      sortText, which is how a server pins order in VS Code.)

      "truncated" says a cap cut the list, and is false unless you asked
      for one - see dm_set_completion_limit. It is also the flag that
      says whether filtering our list by the typed prefix is still safe:
      over a truncated list it is not, because the item your user is
      typing toward may have been cut.

      ** DO NOT MERGE `.` AND `:` **
      They are different lists because the compiler checks them differently.
      `:` widens the check to the subtype tree; it does NOT switch checking
      off. Neither list ever contains an unrelated type's members.

      An empty list after `.` means we could not work out the receiver's
      type - typically a call result or an index. Showing everything there
      would be noise, so we show nothing.

      ** WE INFER FURTHER THAN THE COMPILER DOES. READ THIS ONE. **

      dm.exe has NO local type inference. This:

          var/x = new /obj/item
          x.hp

      is a compile error - "x.hp: undefined var" - even though hp is the
      right member of the type on the line above. Only a WRITTEN type is
      ever checked, so var/obj/item/x compiles and var/x does not, whatever
      it was initialised with. Same for `as` clauses and for assignment
      from an already-typed variable. Verified against 516.1666; the full
      matrix is in docs/dm-language-notes.md.

      We offer those members anyway. An untyped `var/x = new /obj/item` is
      almost always a declaration someone is halfway through writing, and
      the members we list are the ones they are reaching for.

      THE CONSEQUENCE FOR YOU: a user can accept one of our completions and
      get code that does not build. Every such item says so: "inferred":
      true means the receiver's type was worked out rather than written
      down, so badge those items, rank them lower, or drop them - the
      decision is yours and the flag is the fact it needs. No guessing
      from the context or the detail field.

      Everything else in the list agrees with the compiler. This is the one
      exception, and it is confined to completion.

      ** "typeFrom" SAYS WHICH ROUTE PRODUCED THE TYPE **        NEW in 0.22
      A word, beside "inferred", for clients that want to word the warning
      accurately:

        "written"       a declared type - the only one dm.exe checks. A
                        typed GLOBAL counts: a root `var/obj/machine`
                        types `machine.` (0.26)
        "initializer"   an untyped local's `= new /obj/item`
        "assignment"    the nearest `x = ...` before the cursor
        "as"            a parameter's `as` clause
        "none"          nothing resolved it

      "as" is why this field exists. The author WROTE `f(n as num)` and
      nothing was guessed, yet dm.exe still refuses members through it,
      because an `as` clause is an input filter for verb prompts rather than
      a type. So "inferred" is right about the compiler and wrong about the
      author, and a client rendering "we guessed this" over the author's own
      words is telling them something false. Word that case as "you wrote
      this and the compiler will not enforce it".

      "bareTypeName" existed until 0.26: `mob.` used to offer /mob's members
      though `mob.loc` is "undefined var" to dm.exe - a bare type name is
      neither a variable nor a path. That fallback is gone; `mob.` answers
      an empty list unless a var of that name is in scope, which is the var
      resolving (root `var/mob/mob` is idiomatic DM and compiles). If you
      switched on the word, the arm is dead, not broken.

      Handle an unknown word by treating it as "none".

      ** "type" AND "value" ARE THE ITEM'S OWN **               NEW in 0.21
      The declared type and the initialiser as written, per item, so you can
      render a list without re-parsing the file:

        var/mob/M           -> "type": "/mob",  "value": ""
        var/fatigue = 6     -> "type": "",      "value": "6"
        var/list/L = list() -> "type": "/list", "value": "list()"

      Note the second one. EMPTY IS THE HONEST ANSWER, not a gap: DM has no
      `num` or `text` to name, and an initialiser does not type a variable -
      `var/x = new /obj/item` leaves x untyped as far as dm.exe is concerned.
      So for an untyped var the value is what a reader wants, and inventing a
      type there would be us claiming something the compiler does not.

      A parameter's `as` clause is NOT reported as a type. `f(n as num)` gives
      n no type the compiler will check members through - it is an input
      filter for verb prompts - so it stays in "detail", where it reads as
      what it is. The same clause on a proc's RETURN type is checked, and that
      one does resolve.

      "value" is source text, not an evaluated result: `5 + 1` stays `5 + 1`
      and a macro shows as its expansion. Folding waits on a constant
      evaluator; until then this is the author's text rather than a claim
      about what it comes to.

      "documentation" carries the /// comment above the declaration, so a
      popup can show what a proc is for without a second call. Empty for
      builtins - nothing declares them - and empty when there is no comment.
      Both DM forms are read: a run of /// lines, and a /** ... */ block.
      A plain // or /* is NOT documentation - treating one as such would
      attach commented-out code to the declaration below it.

      Tags like @param pass through as text. DM has no doc-comment standard
      and real code spells them several ways, so render them yourself if you
      want them highlighted.

      A proc's "detail" is its signature as declared, with parameter types,
      `as` clauses and DEFAULTS: heal(mob/target, amount as num, silent = 0).
      A default tells your user the argument is optional and what it falls
      back to, which is the same information an @param comment would carry
      and cannot go stale.

      "builtin": true marks BYOND's own members rather than the project's.
      Style them apart if you like; the flag is there for that.

      ** COST ** The first call after an edit rebuilds the object tree for
      the whole project. Debounce it. Do not call it per keystroke.

  dm_workspace_symbols(ws, query, limit, encoding, &json)       NEW in 0.8
      Every symbol in the project whose name matches, as UTF-8 JSON. YOU
      FREE IT with dm_free.

      RANKED AND CAPPED, not exhaustive. A two-character query on a real
      project matches tens of thousands of symbols; unranked that is a wall,
      not a picker. Order is exact name, then prefix, then substring, with
      shorter names first inside each band. Matching is case-insensitive.

      Pass 0 for limit to take the default of 200. Ask for what you will
      actually display.

      Builtins are never returned - nothing declares them, so a hit could
      not be opened.

      "detail" is the owning path, which is what tells two `New`s apart in a
      list. Each hit carries the same two ranges as dm_document_symbols plus
      its file; navigate with the sel* range.

  dm_hover_at(ws, file, line, character, encoding, &json)        NEW in 0.7
      What to show in a tooltip, as UTF-8 JSON. YOU FREE IT with dm_free.

      Returns {} - an EMPTY OBJECT - with DM_OK when nothing resolves. A
      pointer resting on a local, a keyword or whitespace is the ordinary
      case, not a failure, so test for the "detail" key rather than the
      status code.

      Keys: detail (the resolved path), signature (the declaration as
      written), reference (a DM Reference URL, see below), documentation
      (the doc comment above it, markers stripped, joined with newlines),
      and the range of the token you hovered so you can highlight it.

      "reference" is a link into BYOND's own DM Reference for a BUILTIN the
      reference documents - https://www.byond.com/docs/ref/info.html#/mob/
      var/client and the like - and is EMPTY for everything else. A project
      symbol has a declaration to open, which beats any link, and a builtin
      whose anchor the scrape never found gets nothing rather than a guessed
      URL. Render it as a "read more" link beside the signature; an empty
      string means show nothing.

      Both DM doc forms are read: a run of /// lines and a /** ... */ block.
      A blank line or a plain // comment ends the doc run, which matches
      what a reader takes to be attached to the declaration. A plain // or
      /* is not documentation - treating one as such would attach
      commented-out code to whatever follows it.

      Where a symbol has several declarations this renders the NEAREST.
      Hover is a glance; dm_definition_at is what returns the whole chain.

      A MACRO NAME hovers as its #define line, doc comment included -
      detail "#define AMMO_MAX", signature "#define AMMO_MAX 30". Same
      rule as definition: project macros only, not the built-in seeds or
      injected -D defines.

      A BUILTIN hovers too, even though nothing declares it: the
      signature is rendered from the symbol table - "Move(NewLoc, Dir=0,
      step_x=0, step_y=0)" for a proc, "var/loc" for a var, and the
      global `world` answers "/world" with "var/world/world". Its
      documentation is empty and dm_definition_at still returns nothing
      for it - there is no file to open, and hover saying what a symbol
      IS does not change that.

  dm_signature_at(ws, file, line, character, encoding, &json)    NEW in 0.12
      Which call encloses the position, whose proc it is, and which
      parameter the caret sits in, as UTF-8 JSON. YOU FREE IT with dm_free.
      Line and character are ZERO-BASED.

      Ask when the user types `(` or `,`, and re-ask while an argument list
      stays open. Returns {} - an EMPTY OBJECT - with DM_OK when no call
      encloses the position, so test for the "name" key rather than the
      status code.

      Keys: detail (the owning path), name, label (the whole signature as
      one line), parameters (each rendered as declared - type, `as` clause
      and default included - and each a substring of label, so you can
      highlight the active one by search), and activeParameter, zero-based.

      ** IT IS EXACT MID-KEYSTROKE. ** The enclosing call and the parameter
      count come from a scan over the tokens, not from the AST and not from
      counting text commas - so `f(a,` answers correctly while the parser
      only sees it through error recovery, a comma inside a string is not a
      separator, and a nested call or index never miscounts the parameter.
      DM has no overloads, so there is exactly one signature per site.

      ** COST ** Same as dm_complete_at: the first call after an edit
      rebuilds the object tree. The token scan itself is per-file and cheap.

  dm_rename_at(ws, file, line, character, encoding, new_name, &json)
                                                                 NEW in 0.27
      Renames the symbol at the position: the provable edits, plus the
      "uncertain" sites rename refuses to guess about. UTF-8 JSON, YOU FREE
      IT with dm_free. Line and character are ZERO-BASED; spans in the
      answer come back in the encoding you passed, spelled as the reference
      query spells them (startLine/startChar/endLine/endChar over the OLD
      name - write new_name there).

      ** BEST-EFFORT BY DESIGN, AND THE UNCERTAIN LIST IS THE POINT. **
      A sound rename is impossible in DM: `:` searches the whole subtype
      tree and call("name") / text2path() dispatch on strings. "edits"
      holds only sites PROVEN by the same resolution diagnostics and
      references use; "uncertain" holds every site carrying the name
      without proof, each with a reason word - "colonAccess" (`:`, `?:`,
      `?.`, `::`), "untypedReceiver" (`.` through a call result, index or
      untyped var), "stringLiteral" (a literal whose WHOLE text is the name,
      the spelling vars["health"] and call(g, "attack") use; prose that
      merely contains the word is not flagged, as of 0.28).
      SHOW that list to your user. Applying the edits and discarding it is
      how a game breaks with no error anywhere.

      A refusal is DM_OK with a "refusal" word, because "this cannot be
      renamed" is an answer: "nothingAtPosition" (locals and parameters are
      not indexed), "builtin", "type" (type rename is not built), or
      "invalidName". `proc` and `verb` are ACCEPTED as new names - a var by
      either name compiles, verified against dm.exe. Treat an unknown word
      as refused.

      Renaming any override renames the family: the target is the same
      canonical spelling references use, so ask at any declaration or use.

      ** COST ** One binder walk over every project file, plus the tree
      build if this is the first call after an edit.

  dm_definition_at(ws, file, line, character, encoding, &json)   NEW in 0.6
      Where the symbol at a position is declared, as UTF-8 JSON. YOU FREE IT
      with dm_free. Line and character are ZERO-BASED.

      ** IT RETURNS A LIST, AND SEVERAL RESULTS IS NORMAL **
      DM declares one symbol in several places routinely: a type is reopened
      across files, and a proc has an override chain. We report all of them
      instead of picking one, because the pick would be arbitrary and the
      rest are exactly what a reader wants in an override-heavy codebase.

      Order is NEAREST FIRST - the nearest override is what a call actually
      reaches - so if your editor insists on a single destination, take the
      first and offer the rest rather than dropping them.

      A MACRO NAME resolves to its #define. The preprocessor replaces the
      token before the parser ever sees it, so the macro reading wins over
      every other, wherever the name appears - a member slot, a path
      segment, a directive's argument. Only a #define written in a real
      file is navigable: the built-in TRUE/FALSE/DM_VERSION seeds and
      injected -D defines have no source to open, so they resolve to
      nothing, the same rule that keeps builtins out of every other
      definition answer. The table is the walk's end state - a redefined
      macro shows its last definition, an #undef'd one none.

      An empty array means nothing resolved. That is normal and not an
      error: locals and parameters are not in the object tree.

      Each entry has the same two ranges as dm_document_symbols. Navigate
      with the sel* range so the caret lands on the name.

      "file" is an absolute path, and it is often NOT the file you asked
      about - that is the point.

      ** COST ** Same as dm_complete_at: the first call after an edit
      rebuilds the object tree. Debounce it.

  dm_type_definition_at(ws, file, line, character, encoding, &json)
                                                              NEW in 0.19
      Where the TYPE of the symbol at a position is declared. Same shape
      as dm_definition_at. YOU FREE IT with dm_free.

      One hop further than go-to-definition: on `var/mob/test/M`,
      definition lands on the variable and this lands on /mob/test.

      ** ONLY A WRITTEN TYPE IS FOLLOWED. ** An inferred one would send
      your user's caret into a guess - see the inference note under
      dm_complete_at - so it answers an empty array instead. Empty is
      the ordinary result on an untyped local, not an error.

  dm_folding_ranges(ws, file, &json)                          NEW in 0.19
      Foldable regions for a file. YOU FREE IT with dm_free. Lines are
      zero-based and INCLUSIVE of both ends; kind is "region" or
      "comment" as a WORD, and an unknown kind should fold as "region".

      Built from the AST rather than from indentation, because DM's brace
      blocks and significant indentation nest freely: folding on leading
      whitespace silently drops everything written inside braces, which
      is most macro-generated code.

      Needs no object tree, so it is cheap and answers before the project
      has been walked.

  dm_document_links(ws, file, encoding, &json)                NEW in 0.19
      Resolved #include targets, for clickable navigation. YOU FREE IT
      with dm_free.

      The span covers the path text alone, inside its quotes or brackets,
      so the click target is the filename rather than the whole directive
      line. An include that does not resolve yields NO link - navigation
      that dead-ends is worse than none, and an unresolved include is
      where a reader most wants to notice something is wrong.

      Read off the token stream, so a directive inside a comment is
      correctly not one, and no object tree is needed.

  dm_icon_states(ws, file, &json)                             NEW in 0.24
      Every icon state in a .dmi. YOU FREE IT with dm_free.

        { "isDmi": true, "width": 32, "height": 32,
          "states": [ { "name": "door_opening",
                        "dirs": 4, "frames": 6,
                        "delays": [2,2,2,2,2,2],
                        "movement": false, "rewind": false, "loop": 1,
                        "hotspot": [] } ] }

      ** A NAME IS NOT A KEY **
      One name may appear TWICE - once still, once with "movement": true -
      and DM picks between them by whether the atom is moving. 34 of 352 real
      icons do this, so a dictionary keyed by name silently drops half of
      those states. Key on (name, movement), or keep the array.

      An empty name is the DEFAULT state and is completely ordinary: 226 of
      those 352 icons have one. Render it as "(default)" rather than skipping
      it.

      "width" and "height" are 0 when the file did not state them, which 133
      of 352 do not. BYOND's default is 32x32 and we do NOT fill it in - a
      reader inventing it would be indistinguishable from a file that said so.

      ** "isDmi": false IS AN ANSWER, NOT AN ERROR **
      DM_OK with an empty state list means the file is there and is not an
      icon: a zero-byte .dmi - three of one real project's 166 - or a plain
      PNG saved under that extension. Tell your user the asset is broken.
      DM_ERR_NOT_FOUND means the file itself is missing, which is a different
      thing to say.

      Read from disk, not from a buffer: a .dmi is a PNG, and dm_set_buffer is
      for text you are editing. We do not decode the image - you want the
      state list, and if you are drawing one you already have a PNG decoder.

  dm_document_colors(ws, file, encoding, &json)               NEW in 0.23
      The colours written in a file, with the text to write if your picker
      changes one. YOU FREE IT with dm_free.

        { "colors": [ { "startLine": 1, "startChar": 9,
                        "endLine": 1,   "endChar": 18,
                        "red": 255, "green": 0, "blue": 128, "alpha": 255,
                        "form": "literal",
                        "presentations": ["\"#ff0080\"",
                                          "rgb(255, 0, 128)"] } ] }

      Components are 0-255, which is what DM writes and reads - rgb() takes
      0-255 and returns #RRGGBB. Divide by 255 if your picker wants floats.

      The range covers the WHOLE colour as written, quotes included, so
      replacing it with a presentation leaves no stray quote behind.
      "presentations" is what to write, nearest spelling first: the form it
      is already in leads, so accepting a colour does not silently rewrite an
      rgb() call into a literal or the reverse. "form" is "literal" or "rgb".

      ** TWO RULES THAT ARE NOT WHAT YOU WOULD GUESS ** and both were read off
      the compiler rather than the reference:

        - A short form DUPLICATES each digit. "#f08" is 255, 0, 136, which
          rgb2num confirms. Shifting left by four gives 128 and a swatch
          visibly darker than the colour the author wrote.
        - rgb() CLAMPS out of range and TRUNCATES a fraction:
          rgb(300,-20,0) is #ff0000 and rgb(1.4,1.5,1.6) is #010101, so 1.5
          goes to 1 rather than 2.

      Four things are deliberately NOT reported, because answering would mean
      guessing: an interpolated string, an rgb() whose arguments are not plain
      numbers, any call carrying a `space` or named argument (a different
      colour space entirely), and a named colour such as "red". All four are
      real DM. A wrong swatch reads as our bug; a missing one reads as
      unfinished, which is the honest state.

      ** COST ** None to speak of: this needs no object tree, so it answers
      before the project has been walked and costs one pass over the file's
      tokens.

  dm_file_in_project(ws, file)                                NEW in 0.19
      Whether the .dme's include walk actually reaches this file: 1 in
      the project, 0 outside it, -1 on a bad handle or path.

      ** THIS IS THE ANSWER TO A CONFUSION THAT COST A CLIENT A LIVE
         DEBUGGING SESSION **
      dm_set_buffer accepts ANY path and returns DM_OK. But a buffer only
      joins the object tree if the walk asks for that path. So a file the
      .dme does not include analyses fine for everything per-file -
      outline, colours, syntax diagnostics - while its own procs and vars
      resolve to NOTHING, and symbols from project files resolve normally
      in the same buffer. That asymmetry reads exactly like a broken push.

      Ask this before blaming your own code, and tell the user "this file
      is not part of the project" rather than drawing an empty outline
      that looks like a failure. Free once a tree exists: the walk
      already produced the list.

  dm_dme_is_ticked(ws, file)                                  NEW in 0.20
  dm_dme_tick(ws, file, &json)
  dm_dme_untick(ws, file, &json)
      Read and edit DreamMaker's own // BEGIN_INCLUDE block - what its
      checkbox does. is_ticked returns 1 / 0 / -1; the other two return
      an edit as UTF-8 JSON, which YOU FREE with dm_free.

      ** WE RETURN THE EDIT, WE DO NOT WRITE THE FILE **
      The .dme is usually open in the editor that asked, and often has
      unsaved changes; writing it underneath you would lose them. A tick
      is a zero-length insert, which applies cleanly to a buffer somebody
      else owns. Offsets index the .dme text THIS WORKSPACE CURRENTLY
      SEES - so if you hold unsaved changes, push the .dme with
      dm_set_buffer first and apply the edit to that same text.

      "refusal" is always present, as a word: "none" (an edit follows),
      "noChange", "noBlock", or "conditional". The last one is a refusal
      on purpose - a line inside an #if does not mean the file is in the
      build, so neither edit has a correct answer and guessing at your
      project file is the wrong move. Show the reason rather than
      swallowing it.

      Sort order is DreamMaker's own, which is not the obvious one: files
      before directories, extension before filename, and ordinal
      comparison on the lowercased string. It is verified against real
      .dme files rather than reasoned about, so a ticked file lands where
      DreamMaker would put it and the block does not churn.

      The full contract, including the double-listing case, is in
      dm_core.h.

  dm_classification_count(spans)     -> N, or -1 if the handle is invalid
  dm_classification_data(spans)      -> pointer to 3*N int32s, or NULL
  dm_classification_free(spans)      -> release it

      The data block is (offset, length, kind) repeated N times, contiguous.
      memcpy it if you want, it is plain int32s. Valid until you call free.

  dm_query_json(ws, request, &json)                            NEW in 0.11
      A bulk question about the object tree, as UTF-8 JSON in and out. YOU
      FREE IT with dm_free. The full shapes are in abi/dm_core.h and frozen
      in abi/schema/; this is what they are for.

      THIS IS WHAT YOUR TREE PANEL RUNS ON. The position-shaped calls answer
      about a caret; these answer about a PATH, and return a lot at once.

        {"query":"objectTree","path":"/obj","depth":1}
            one node and its children. depth 1 is a node the user just
            expanded, depth 0 is a single-row refresh
        {"query":"subtypesOf","path":"/obj","limit":500}
            everything beneath a path, flat, in tree order
        {"query":"members","path":"/mob","inherited":true}
            a type's vars and procs, with inheritance already resolved -
            each one says which type it came from
        {"query":"ancestorsOf","path":"/mob"}                    NEW in 0.14
            the whole inheritance chain, nearest first, self excluded, in
            one call instead of one objectTree round trip per level
        {"query":"references","path":"/mob/hp","limit":1000}     NEW in 0.14
            THE REFERENCE INDEX: every use of a symbol. The path is the
            detail string a dm_definition_at hit gives you (take the LAST
            hit - it is the canonical declaration): "/mob/hp" for a var,
            "/mob/heal()" for a proc, "/heal()" for a global, a type's
            path for a type. Each hit:

              { "file": "code/mob.dm",
                "kind": "write",           read | write | call | override
                "inside": "/mob/hurt()",   the enclosing symbol
                "startLine": 3, ... }      positions per the request's
                                           "encoding": "utf16" (default)
                                           or "utf8"

            "override" hits are proc declarations overriding the target -
            what-overrides-this, the safety question before changing a
            proc's behaviour. Group hits by "inside" for a call hierarchy.
            Locals and parameters are not index symbols; hits exist only
            where the diagnostics' own resolution succeeds, so the list
            under-reports rather than lies (an unresolvable-receiver use
            is invisible to it, same as it is unchecked by dm.exe).

      Two fields exist so you do not have to make a second call or a wrong
      guess:

        "childCount" is how many children EXIST, which is not the length of
        "children" when depth cut the response short. Draw your expander
        arrow from this. Without it, every collapsed row costs a round trip.

        "truncated" says the limit cut the list. A list exactly as long as
        the limit looks identical to one that was cut, so do not infer it
        from the count - if you do, your picker will claim a type has 500
        subtypes when it has 4,000.

      "parentType" is where a type INHERITS from, which is not always its
      path parent: /mob is a child of the root by path and inherits from
      /atom/movable. Draw the tree by path and show parentType separately,
      or your panel will describe a different program from the one that runs.

      Unknown members in a request are ignored, so a client written against
      a later schema still gets an answer from an older library. An unknown
      query name or a malformed request is DM_ERR_INVALID_ARG; a path that
      is not in the tree is DM_ERR_NOT_FOUND. An empty array with DM_OK is
      an answer - a type with no subtypes.

      ** COST ** Same as dm_complete_at: the first call after an edit
      rebuilds the object tree. Debounce it, and prefer one deep query to
      many shallow ones while typing.

  dm_workspace_close(ws)
      Done. Safe to call on an already-closed handle.


--------------------------------------------------------------------------------
 5. ENCODING - READ THIS ONE
--------------------------------------------------------------------------------
  Every call that deals in offsets takes an encoding. There is no default and
  we will not guess.

    DM_ENCODING_UTF16   offsets count UTF-16 code units
    DM_ENCODING_UTF8    offsets count UTF-8 bytes

  WHICH DO I WANT?

    Qt / QString              -> DM_ENCODING_UTF16
    C# / .NET string          -> DM_ENCODING_UTF16
    Java / JavaScript string  -> DM_ENCODING_UTF16
    std::string / raw bytes   -> DM_ENCODING_UTF8
    Rust String / &str        -> DM_ENCODING_UTF8
    Python 3 str              -> neither is exact; use UTF-8 and index bytes

  ** WHY THIS WILL BITE YOU **

  For pure ASCII the two are IDENTICAL. So if you pick the wrong one,
  everything works. Your tests pass. You ship it.

  Then a user types a comment with an accented character, or an emoji, or CJK
  text in a string literal - and every colour on that line after it shifts by
  one to three characters. It will look like a bug in our lexer. It is not.

  Pick the right one now. If you are unsure, write one test with a non-ASCII
  character in it and check the colours still line up.


  LINE ENDINGS - THE OTHER TEXT TRAP, AND THIS ONE IS YOUR PROBLEM NOT OURS
  --------------------------------------------------------------------------
  DM files are usually CRLF on Windows. Editor frameworks normalise hard:
  Qt's QTextDocument and QPlainTextEdit::toPlainText() always hand you \n
  whatever was on disk, and QIODevice::Text translates on read.

  FOR US THAT IS HARMLESS, as long as you push your buffer via dm_set_buffer.
  We then analyse exactly what your editor displays, and every position we
  return is line/column, which a \r cannot shift. SourceText treats \r\n,
  bare \n and a lone \r as terminators, and \r never appears inside token
  text.

  FOR YOU IT IS NOT HARMLESS, for three reasons:

    1. Reading with translation and writing without it rewrites EVERY LINE
       on save. Whole-file diffs, and git blame is dead for the rest of the
       team.
    2. DM's {" multiline "} strings carry their newlines as CONTENT.
       Converting endings inside one changes program data, not formatting.
    3. Offsets computed from your buffer will not match offsets into the
       file on disk.

  DO THE ROUND TRIP YOURSELF. No editor framework does it for you:

    - open with QFile::ReadOnly WITHOUT QIODevice::Text, so bytes arrive raw
    - sniff the dominant ending, store it per document
    - normalise to LF internally - correct, and what this library wants
    - re-apply the stored ending on save
    - put "*.dm text" in the game repo's .gitattributes so git agrees


--------------------------------------------------------------------------------
 6. COLOURS
--------------------------------------------------------------------------------
  `kind` is one of these. Values are a permanent contract and are never reused.

     0  DM_CLASS_NONE                     default foreground
     1  DM_CLASS_COMMENT                  // and /* */
     2  DM_CLASS_KEYWORD                  var, if, for, src, return, ...
     3  DM_CLASS_IDENTIFIER               everything else that is a name
     4  DM_CLASS_NUMBER                   42, 3.5, 0x1F, 1e10
     5  DM_CLASS_STRING                   "..." and {"..."} incl. the quotes
     6  DM_CLASS_INTERPOLATION_DELIMITER  the [ and ] inside a string
     7  DM_CLASS_RESOURCE                 'icons/mob.dmi'
     8  DM_CLASS_OPERATOR                 + - * / = < > etc
     9  DM_CLASS_PUNCTUATION              ( ) [ ] { } , ;
    10  DM_CLASS_PREPROCESSOR             #define, #include, #if ...
    11  DM_CLASS_ERROR                    we could not understand this

    SEMANTIC - produced now:
    12  DM_CLASS_TYPE_NAME                a path segment naming a real type
    13  DM_CLASS_PROC_NAME                a name followed by (
    14  DM_CLASS_VAR_NAME                 a member read with no call parens
    15  DM_CLASS_MACRO_NAME               a name the project #defines

  ABOUT 12-15
    These only ever REFINE a span the lexical pass already produced. No span
    is added, removed or moved, so if you ignore them you get exactly the
    output you got before - which is why mapping them was safe to do early.

    They are deliberately conservative. A wrong colour reads as a bug in us;
    a missing one reads as "not implemented yet", so we under-claim. A bare
    "mob" stays an identifier, because it is more often a variable than the
    type.

    12 needs the object tree, and CLASSIFICATION WILL NOT BUILD ONE - a
    whole-project walk on your paint path would be a bad trade. So type
    names stay plain identifiers until something else builds a tree
    (dm_complete_at, dm_document_symbols), and light up from then on. If you
    want them immediately, call dm_complete_at once after opening.

  Handle unknown values by falling back to the default colour. That way a
  library update cannot break your renderer.

  NOTES ON SPECIFIC KINDS

    INTERPOLATION_DELIMITER is separate from STRING so that "[src.name]" reads
    as string, bracket, code, bracket, string. Colour the brackets like an
    operator and the hole reads as code, which is what people expect.

    Spans do NOT cover whitespace. The gaps between spans are whitespace and
    layout - paint them with your background.

    Touching spans of the same kind are already merged. A three-token string
    comes back as one span, not three.


--------------------------------------------------------------------------------
 7. ERRORS
--------------------------------------------------------------------------------
  Every function that can fail returns dm_status. DM_OK is 0.

    DM_OK                    0
    DM_ERR_INVALID_ARG       1   you passed a null or nonsense argument
    DM_ERR_INVALID_HANDLE    2   handle is closed, freed, or garbage
    DM_ERR_NOT_FOUND         3   file does not exist and has no buffer
    DM_ERR_OUT_OF_MEMORY     4
    DM_ERR_INTERNAL          5   our bug. please report with the message.

  char *msg = dm_last_error();   // may be NULL. YOU FREE IT with dm_free.

  dm_last_error is per-thread and reflects the last failing call on that
  thread.

  Out-parameters are always cleared before we do any work, so reading them
  after an ignored error gives you NULL rather than uninitialised stack.

  WE NEVER THROW ACROSS THE BOUNDARY. An exception escaping into your process
  would kill your IDE, so every entry point converts failures into a status
  code. If we ever crash your process, that is a bug worth reporting loudly.


--------------------------------------------------------------------------------
 8. THREADING
--------------------------------------------------------------------------------
  Current contract: ONE WORKSPACE IS USED FROM ONE THREAD AT A TIME.

  This is not enforced, and violating it will corrupt state rather than fail
  cleanly. If you analyse on a worker thread, either keep all calls on that
  thread or hold a mutex.

  Different workspaces on different threads is fine.

  Background analysis with callbacks is a later feature. Ask if you need it
  sooner and we will move it up.


--------------------------------------------------------------------------------
 9. WHO FREES WHAT
--------------------------------------------------------------------------------
  Anything we hand you, you release. Nothing we hand you points into memory we
  might move.

    char *          from dm_last_error, dm_workspace_root  -> dm_free()
    dm_classification                                      -> dm_classification_free()
    dm_workspace                                           -> dm_workspace_close()

  Anything you hand us is copied before the call returns. Free your own copy
  whenever you like.

  USING A FREED HANDLE IS SAFE-ISH BY DESIGN. A handle is an opaque id that is
  never reused, so a stale one returns DM_ERR_INVALID_HANDLE (or -1 / NULL)
  rather than silently resolving to whatever was opened next. Do not rely on
  this, but it means a use-after-free in your code is a clean error you can
  find, not memory corruption you cannot.

  Treat the value as opaque. It is not a pointer, not an index, and carries no
  structure worth reading - it changed shape once already, when the previous
  encoding turned out to need 64-bit pointers and silently did not have them on
  win-x86.

  Closing twice is a no-op, not a crash.


--------------------------------------------------------------------------------
 10. PERFORMANCE
--------------------------------------------------------------------------------
  What actually happens when you call dm_classify_range:

    - if the buffer changed since last time, the WHOLE FILE is re-lexed
    - the result is cached on the document
    - only your requested line range is packed and returned

  Why the whole file: a {" multiline string "} or a nested /* comment */ can
  open thousands of lines above your visible range, and it decides whether
  line 900 is code or text. Lexing just the visible range gives wrong colours.

  Practically: cost is paid once per edit, not once per scroll. Scrolling a
  large file re-uses the cached lex and is cheap.

  Do not call dm_set_buffer with identical text on every paint. That throws
  away the cache and re-lexes. Call it when the text actually changes.

  WHAT AN EDIT COSTS NOW, WHICH IS THE NUMBER BEHIND EVERY "DEBOUNCE IT"
  ABOVE. The calls that need the object tree - dm_complete_at,
  dm_definition_at, dm_hover_at, dm_workspace_symbols, dm_query_json - used
  to rebuild the entire project on the first call after any edit. They no
  longer do. A file is re-read, re-walked and re-parsed only when its text or
  the macro state reaching it actually changed, which after one keystroke is
  one file:

    100 files    (a real game)        131 ms  ->   10 ms
    7,161 files  (/tg/station)         13.7 s  ->  909 ms

  Both figures are one keystroke, warm, Release, and both were verified
  against a build of the same text with every cache off - 335,519
  declarations identical on the large one. Editing a #define is the
  expensive case by design: everything downstream of it genuinely changed,
  so that one edit costs a full rebuild and the next is fast again.

  What is still whole-project is the final merge into the object tree, which
  is most of what remains on the large figure. So: still debounce, but the
  budget you are debouncing against is tens of milliseconds on a normal
  project rather than a rebuild you can see.

  You need do nothing to get this. We revalidate each file against its
  last-write time and length rather than trusting the cache, chosen so that a
  git checkout under your running IDE is still seen - no API call, nothing to
  remember, and no staleness if a build script rewrites files behind you.

  Incremental re-lexing WITHIN a single edited file (only the changed region)
  is still not done. Say something if you hit a file where re-lexing the whole
  buffer per keystroke is too slow, ideally with the file size.


--------------------------------------------------------------------------------
 11. TROUBLESHOOTING
--------------------------------------------------------------------------------
  Build fails copying dm_core.dll, or the DLL vanishes after publishing
      Windows Defender can quarantine a freshly built NativeAOT binary as a
      false positive. Seen on this project: the publish reports success, then
      dm_core.dll is gone from both publish/ and native/, and CMake fails on
      the copy step with a wall of MSB3073 text that says nothing about
      antivirus. Check with:
        Get-MpThreatDetection | Select-Object -Last 1 | Format-List Resources
      If your own build output is listed, add a Defender exclusion for the
      repo directory. This will hit CI on Windows runners too.

  "unresolved external symbol dm_..."
      You are not linking dm_core.lib, or you are linking a stale one.
      On Windows you link the .lib and ship the .dll. Both.

  Program starts and immediately exits, code 0xc0000135
      dm_core.dll is not next to your executable.

  dm_workspace_open returns 3 (NOT_FOUND)
      The .dme path is wrong. Check dm_last_error for the resolved path we
      actually tried.

  Everything returns 2 (INVALID_HANDLE)
      You are passing a handle that was closed, or never initialised. Note we
      clear out-params on failure, so an ignored error leaves you holding NULL.

  Colours are right on ASCII lines and wrong after accented / CJK characters
      Wrong encoding. See section 5.

  Colours are right, but shifted by a line
      Line numbers are ZERO-based here. Editors often display 1-based.

  A whole file colours as one giant string
      An unterminated {" ... "} somewhere above. That is correct behaviour -
      it is also what the DM compiler sees. Check for a stray {" .

  Whole file colours as a comment
      Same thing with /* and no */. DM nests block comments, so an extra /*
      inside a comment needs its own */.


--------------------------------------------------------------------------------
 12. HOW TO CHECK OUR OUTPUT
--------------------------------------------------------------------------------
  If you think we are classifying something wrong, check with the CLI before
  digging into your own code. It uses the exact same code path as the ABI.

    dotnet run --project src/Dm.Cli -- classify <file.dm>
        renders the file with colours in your terminal

    dotnet run --project src/Dm.Cli -- classify <file.dm> --spans
        the raw span table, with BOTH utf16 and utf8 offsets side by side.
        very useful for confirming an encoding problem.

    dotnet run --project src/Dm.Cli -- dump-tokens <file.dm>
        the raw token stream, if you want to see why a span came out that way

    dotnet run --project src/Dm.Cli -- scan <file-or-directory>
        reports anything the lexer did not recognise across a whole codebase

    dotnet run --project src/Dm.Cli -- outline <file-or-directory>
        the declarations we found in a file

    dotnet run --project src/Dm.Cli -- symbols <file>
        the document outline: kind, name, detail, and the line:col of each
        name. this is the exact shape the ABI call will hand you, so you can
        design your outline pane against it today
        --utf8   columns in UTF-8 bytes, to check your encoding choice

    dotnet run --project src/Dm.Cli -- query <dme> --path /obj --depth 2
        the same answers dm_query_json gives, rendered as a tree
        --subtypes <path> / --members <path> for the other two queries

  WHEN A TYPE OR MEMBER IS MISSING AND YOU WANT TO KNOW WHY, these three walk
  the same pipeline we do, one stage at a time. Work down the list - the first
  one that looks wrong is the stage that broke.

    dotnet run --project src/Dm.Cli -- includes <dme>
        every file the project compiles, IN COMPILE ORDER. if a file is not
        here, nothing in it exists to us
        --tree      show the include nesting
        --orphans   .dm files on disk the .dme never reaches
    dotnet run --project src/Dm.Cli -- preprocess <dme>
        the whole project expanded, in compile order
        --macros    which macros produced how many tokens
        --dump      every token, showing where it traces back to
    dotnet run --project src/Dm.Cli -- tree <dme>
        the object tree, built in compile order
        --under <path>     what is declared beneath a type
        --members <path>   one type's members, inherited included
        --problems         parse diagnostics grouped by message

    dotnet run --project src/Dm.Cli -- complete <dme> <file> <line> <col>
    dotnet run --project src/Dm.Cli -- definition <dme> <file> <line> <col>
    dotnet run --project src/Dm.Cli -- hover <dme> <file> <line> <col>
    dotnet run --project src/Dm.Cli -- signature <dme> <file> <line> <col>
    dotnet run --project src/Dm.Cli -- icons <file-or-directory>
        icon states, as dm_icon_states answers them. Point it at your whole
        icons/ folder and it totals them, which is how the reader was checked
        against 352 real files
    dotnet run --project src/Dm.Cli -- colors <file>
        the colours we find in it and what a picker may write back. No .dme
        needed - colours come off the token stream
    dotnet run --project src/Dm.Cli -- hints <dme> <file>
    dotnet run --project src/Dm.Cli -- references <dme> <file> <line> <col>
        --path /mob/hp   ask by symbol instead of by position
    dotnet run --project src/Dm.Cli -- wsymbols <dme> <query>
        the same answers dm_complete_at, dm_definition_at, dm_hover_at,
        dm_signature_at, dm_inlay_hints, dm_query_json's references and
        dm_workspace_symbols give you.
        ** LINES AND COLUMNS ARE 1-BASED IN THE CLI AND 0-BASED IN THE ABI **
        so add one when reproducing what your editor asked for.

    Every command that reads a .dme takes -DNAME as well, so you can
    reproduce a query with the same defines you pass to dm_set_defines.

  RULE OF THUMB: if the CLI reproduces the problem, it is ours - report it.
  If the CLI looks right and your editor does not, it is in your integration.


--------------------------------------------------------------------------------
 13. WHAT'S COMING, AND ROUGHLY WHEN
--------------------------------------------------------------------------------
  Order is fixed; timing is not. See ROADMAP.txt for live status.

    M3   preprocessor + #include graph      DONE. no API for you, but
                                             everything after it depends on it
    M4   parser -> document outline, real syntax errors      DONE
    M5   object tree + BYOND builtins                        DONE
    M6   autocomplete                                        DONE
         including type inference through new/as/assignment, macros in the
         bare-identifier list, and semantic highlighting (colour IDs 12-15)
    M7   go to definition, hover, workspace symbols,
         object tree bulk queries                            DONE
         that is team v1: everything an IDE needs for its panels
    M9   incremental reparse + performance                   DONE
         no API change. after an edit we re-read, re-walk and re-parse only
         the files whose text or macro state actually changed - 1 of 7,161
         on a project that size. see section 10 for what this means for you
    M8   .dmi icon states                                    DONE
         dm_icon_states at 0.24, LSP dm/iconStates, dmc icons. Passed over
         at the M8/M9 fork and picked up five milestones later; built
         against 352 real icons, which is where the contract above comes
         from - zero-byte .dmi files, repeated state names, empty names
    M10  LSP server, for editors that speak it              DONE
         Dm.Lsp serves diagnostics, completion, hover, signature help,
         definition, symbols, semantic tokens, inlay hints, folding,
         document links, $/cancelRequest, $/progress and the
         dm/objectTree family (same shapes as dm_query_json) over stdio;
         editors/vscode/README.md is the five-minute recipe for running
         it against a real game, and docs/lsp.md is the protocol for
         every other client - launch, initializationOptions, the dm/*
         methods, and what is deliberately not served. Incremental document sync is the one
         deliberate omission - full sync costs one string against a
         ~10 ms rebuild, so it waits for a profile to ask. The
         same core answers both shells, so nothing here changes for you
         - docs/capability-matrix.md tracks the parity.
    M11  semantic diagnostics, find-references, rename       STARTED
         the differential harness is in: we run dm.exe over a project and
         diff its diagnostics against ours, so every check added from here
         is measured against the compiler from its first commit. the
         semantic set ships through dm_diagnostics (0.13) - new checks
         arrive there with no ABI change. Find-references SHIPPED with the
         reference index (0.14) and rename SHIPPED at 0.27, best-effort
         with the uncertain sites reported; both are in section 4

  If your ordering needs differ from this, say so.


--------------------------------------------------------------------------------
 14. REPORTING A PROBLEM
--------------------------------------------------------------------------------
  Useful:
    - the smallest .dm file that shows it
    - what you expected vs what you got
    - output of: dmc classify <file> --spans
    - dm_abi_version() you are running against

  Not needed:
    - your editor's code, unless the CLI disagrees with it
================================================================================
